# BUILD_STATE — THE v2 SEALED STATE (2026-08-25)

**Dist:** f41153fb9cb22d66b900a1db0fc99401875dc50d233224e23178142091be43b7 · **Battery:** 633/633 · **tsc:** clean
**Checkpoint:** `Checkpoints/wave-manager-generate-SHIP-APPROVED-v2/` — src + dist + docs + logs fully synced
**Git:** 4.4.2 @ `f786dad` (the key-scrubbed HEAD) · **Host deploy:** f41153fb verified
**Docs:** DEBUG_LOG EN 1-183 · BUILD_REPORT + the v2 addendum · the canon set current

## THE BUILD LAYERS (what exists, in dependency order)

1. **THE GENERATION CORE** — the shadow pipeline (13 stages) on the GO-primary chain; the spec-file-only input; auto-dispatch per completion; the RPM ledger; the stall guard.
2. **THE CONTROL PLANE** — steer soft/hard (STEER-ONLY), pause pure-interrupt, kill ×3, resume-all; the agent-correct deliveries (the title-token detector); the tracker state machine (kill/resume sync + un-archive); the mode-spillover gate.
3. **THE INTEGRITY LAYER** — the return-integrity lexicon (L-TRUNC-1..5 + the state machine + the triads) in both read instruments; the telemetry honesty fix; the W16 return-integrity gate doctrine.
4. **THE SCOPE LAYER** — the workdir-anchored resolver + the session-sticky cache; the projectToken override; the BUN_TEST tracker isolation.
5. **THE IDENTITY** — 23 warheads: [CRITICAL] W16 v2 (the wave-manager execution law: generate/telemetry-guard/stream-truth/control-plane/CTO-loop/scope/audit) + W22 (red-team-by-default) + [CRITICAL] W19.
6. **THE KNOWLEDGE** — the skills (hydra-orchestrator first-class; deep-container-testing v2 two-rig; warhead-writing anti-pattern), the plug-and-play boilerplate, the github README (v2 sections).

## THE SESSION'S SHAS (the deploy chain)

`997603c3` (the GO wiring) → `8041785a` (the crash guard) → `997603..` → `0592c9c4` → `632c21c0` (pure pause) → `00123a2d` (kill-by-session) → `78ef08b2` (the tracker syncs) → `ba46b3be` (resume-all) → `c746f75f` (the mode gate) → `92e63743` (W16 v2 + W22) → `182a12c7` (the CTO loop) → `9cbd8647` (the spillover gate) → `52a73834` (the integrity v1) → **`f41153fb` (the lexicon — THE SEALED STATE)**

## THE OPEN ITEMS

- The git-history orphan-squash (the pre-scrub commits carry real keys) — the operator's call.
- The stale global sidecars (retire with the old sessions' restarts).
- The operator's other-session waves in the prod tracker (theirs to manage).
