# EVIDENCE_STATE — THE VERIFIED EVIDENCE (2026-08-09)

**THE TRUE DIST SHA: 416ccff7b0d8812479ec3f35cc29394ae926eae7e50b583093fadc9431c6919b.** Every "this works" claim below is backed by mechanical proof — the behavioral tokens, the SHAs, the container provenance. Structural greens are never canonized as behavior. THE WARHEAD 13's law: a "container tested" declaration requires the artifact's per-scenario PASS verdicts — an environment-blocked suite is BLOCKED, never tested. THE 2026-08-10 EVIDENCE: S9 PASS (the anti-cuck identity probe — the container's answer on dist 416ccff7: "dispatch parallel trident_explore subagents... I would not read all 258K lines myself" — the WARHEAD 15 mandate executed live).
> **2026-08-13 UPDATE — THIS SESSION:** the wave-manager dispatch-authorization transactional fix + the 2026-08-13 rulings (pool 15, retries 3, timeout 15m, directive IDs, v4-flash pin) + the main-session self-heal + the multi-session anchor + the prune sort fix + the self-heal misfire fix. TRUE DIST: ddc2b24a1a026555c92385f61228a5ff7930db75560443cf7e7d5a1c21042a79. Unit 408/408, tsc 0. See the DEBUG_LOG.md 2026-08-12/13 entries + the BUILD_REPORT addenda for the full record. Host-verified: the exact-bug cycle, the anchor, the re-fire protection, the prune survival, the release-by-alias.

---

## THE SHA CHAIN (the deployment fingerprints — the marathon)

```
e2661142 → 291f9ddb → f90477d7 (WRONG endpoint — reverted) → 8d897fd2 → 04a26dce (endpoint revert)
→ a7b886d3 (FIXED key) → 81b23117 (wave build + CT 4/4) → af10c5a9 (client-null fix)
→ 7b792e2e (visibility fix) → 9eedfdbd (CATASTROPHE-FIX BASELINE — the 7.4 original save)
→ 8da56572 (contamination fix + CT cp/gate fixes) → f71a78b8 (SQLite state + no-fallback + scaffold strip)
→ 166d4b13 (DCS bound + offset/limit + cursors) → b39fc378 (accumulation + no-fallback)
→ c807c172 (384K + parallel + atomic seq + error-mask) → 90b10fb3 (rename)
→ 5868eb7e (either/or) → e38c61c9 (no-fabrication + bracket tolerance + echo strip)
→ 341de6f1 (loud-fail wiring) → adfee70e (firewall re-enable + NO LAZY PROMPTS)
→ 2daaba66 (SSTF arming fix + stall detector) → 6048d43d (stall detector final)
→ 06ac7d22 (hallucination purge + task-preflight removal)
→ 3553470f (THE WAVE-GENERATOR FIXES: the streaming transport + the pool + the telemetry + the retry + the named-partial)
→ 948600a5 (THE THEATRICAL OVERHAUL: the v3 verdict + the tracker + the surfaces + the texts)
→ 361fa522 (the F8 composition fix)
→ acff1eb3 (the THROW-ONLY enforcement)
→ c4cdbe09 (the accumulated-count ESCALATE check)
→ 9f93842b (the message-surface theatrical wiring removed)
→ 43546746 (the production-anchor fix)
→ ff3f6138 (THE WAVE-VERBATIM OVERHAUL: the SHA verification + the batch enforcement + the promptFile + the preservation)
→ 1317662f (the WAVE_RECORD calibration)
→ 8b873cb2 (the lines-gate + the question-ban)
→ 1878ac92 (THE IDENTITY OVERHAUL — the last three-way sync)
→ 21e0a4ad → 3a0d1f1f → 72785f9f (the warheads' finals)
→ c1b80e07 (the WARHEAD 14)
→ 11fb42e7 (THE CURRENT — the WARHEAD 14's shape-correct final)
```

**THE DEPLOYED HOST: the GNR dist** (the operator's revert after the ship rejection) — the deploy of `11fb42e7` is the operator's action.

---

## THE CONTAINER-RUN PROVENANCE (which container, which stream, the behavioral tokens)

### RUN 1 — wavegen-fix-ct, dist 3553470f — THE WAVE-GENERATOR FIXES (6/6 PASS)
| Scenario | The behavioral verdict | The evidence |
|---|---|---|
| S1 the auth probe | PASS | the telemetry `"status": "ok"` + the durationMs 318433 — the generation completed with the key |
| S2 the 2-agent wave | PASS | BOTH agents ok (`failed: []`), the starts 72ms apart — the parallel pool live, the batch form + the `"subagent_type"` |
| S3 the thin-args refusal | PASS | 'too thin' — the CTX_FLOORS before any LLM call |
| S4 the batch-form contract | PASS | "NO subagents have been dispatched" — the generator-only |
| S5 the read regression | PASS | MODULE_LOADED at offset 0 — the DCS-swallow fix holds |
| S6 the switch dual-names | PASS | the switch-agent + the switch-model verified, the status bar matched |

### RUN 2 — theatrical-fw-ct, dist acff1eb3/c4cdbe09 — THE THEATRICAL ESCALATE
| Scenario | The behavioral verdict | The evidence |
|---|---|---|
| the throw-ladder | PASS — THE THROW FIRED LIVE | the 3 completed-message armings (the claim-via-mock + the shim + the emulator reports) → the neutral read call THREW the [TRIDENT THEATRICAL ESCALATE] (the screenshot: "repeated substitute-for-real suggestions (count 15)...") — the tool.before throw, the throw-only enforcement |
| the legit preservation | PASS | the legit jest.mock request in the fresh session — NO throw anywhere |

### RUN 3 — the HOST DIRECT TESTS (the earlier deploys — the [WAVE VERBATIM] + the [WAVE BATCH])
| Scenario | The behavioral verdict | The evidence |
|---|---|---|
| the condensed dispatch | PASS — BLOCKED | the `[WAVE VERBATIM]` tool error on the condensed 'fx-a' dispatch ("the SHA mismatch — the prompt was compressed/condensed") |
| the exact single of the 2-agent wave | PASS — BLOCKED | the `[WAVE BATCH]` tool error ("the wave has 2 agents — a SINGLE task dispatch is the derailment pattern") |
| the legit jest.mock | PASS | the code-unit claim — NO demand (the anti-derail) |

### RUN 4 — theatrical-fw-ct, dist 11fb42e7 — THE DEEP TEST (THE ACTIVE — the honest state)
| Scenario | The behavioral verdict | The evidence |
|---|---|---|
| the fixtures | PASS (ready) | FIX_OK d793aea8bf31 eb27986f1677 — the wave-fx slop manifest + the wave-dpl 130-line DPL1 fixture in the container's tmp |
| the model | PASS (live) | DeepSeek V4 Flash (2x usage) / OpenCode Go — the switch-model verified:true, the status bar matched |
| S3-S6 the firewalls | **NOT RUN — BLOCKED-BY-ENVIRONMENT (the earlier attempts)** | the model providers' exhaustion (the OpenRouter credits, the Zen balance, the Go resolution's earlier fumble) — RESOLVED: the Go model is live now; the deep run's next session executes the 7 scenarios |
| the artifact | the current record | .trident/container-test-results.json — the honest BLOCKED state (the deep test replaces it with the per-scenario PASS verdicts) |

---

## THE ENDPOINT PROBES (the provider diagnosis — the live evidence)

- The minimal request (deepseek-v4-flash, 50 max_tokens, the operator's key): **HTTP 200 in 2.105641s**.
- The EXACT shadow-brain shape (384000 + effort max): **HTTP 200 in 2.329323s**.
- THE STREAMING PROBE (2026-08-09 — the root-cause proof): the streaming request's **first byte in 1.009458s** (the full 1.24MB in 42.1s); the NON-streaming request's **first byte at 48.905152s** (> the 45s stall window — the old transport killed healthy generations); the 3-concurrent non-streaming at 35.9-39.0s each (the queue's geometry).
- **THE VERDICT:** the key/endpoint/model/384K all fine — the stalls were the transport's buffering + the provider's queue; the streaming transport fixed the class.

---

## THE AUDIT FINDINGS (the TRUE stats — never embellished)

### The wave-generator stabilization (the marathon)
- The battery: 175 pass / 0 fail / 638 expect (the LIVE battery — scoped to src/tests).
- The container 6/6 suite (wavegen-fix-ct) + the host live waves (the th-ctx: durationMs 158724).
- The last three-way sync: 1878ac92 (1 unique) — the warhead builds since are UNSYNCED.

### The theatrical incident + the overhaul
- The three holes: the blanket `\bmock\s+server\b` exemption (trident-hooks.ts:400 — now REMOVED), the args-only surface (now + the throw-only), the subject-blind containerTestRan (now + the tracker).
- The 4 live-caught bugs + the fixes: the F8 suppression, the ESCALATE scoping, the history-rescan, the production-anchor — all fixed + the container's ESCALATE-throw proven.

### The wave-verbatim overhaul
- The [WAVE VERBATIM] + the [WAVE BATCH] + the lines-gate + the question-ban + the promptFile + the preservation — the host's direct tests fired the first two LIVE; the deep test's full suite is the ACTIVE verification.

### The identity overhaul
- The warheads 12/13/14 (the operator-approved finals) + the inline wiring gap found + fixed + the warhead-writing skill (the full 12-step workflow + the META-AUDIT).

---

## THE MECHANICALLY PROVEN vs THE MERELY CLAIMED

### PROVEN (the mechanical evidence exists)
- The wave-generator fixes: the container 6/6 + the host live + the streaming probes.
- The theatrical ESCALATE throw: the container's live screenshot.
- The [WAVE VERBATIM] + the [WAVE BATCH]: the host's direct-test tool errors.
- The battery 175/0 + the dist's SHAs (the chain).
- The warheads' landing + the inline regen (the source + the build).

### CLAIMED (the next session must verify)
- **THE DEEP TEST'S S3-S6** (the [WAVE VERBATIM] in the container, the [WAVE BATCH] in the container, the lines-gate slop block, the DPL1 exemption pass) — the fixtures + the scenarios ready + the model live — the run pending.
- The checkpoint/package sync at 11fb42e7 (stale at 1878ac92).
- The host's post-deploy direct tests (the host runs the GNR).

---

## THE CONTAINER-RUN HONEST NOTES

1. The 2daaba66-era generation intermittency was the provider's flakiness — the code surfaced it loudly (the loud-fail law) — the correct behavior.
2. The deep test's earlier blockage was the MODEL PROVIDERS' exhaustion + my config fumbling (the WARHEAD 14's args-check lesson) — RESOLVED: the switch-model with the correct display name (`DeepSeek V4 Flash`, never the `(new)`) landed cleanly.
3. The 341de6f1-era 6/6 was NOT re-run on the final dist — the deep test's S1 (the auth probe) re-verifies the generation machinery.
4. The theatrical + the wave-verbatim + the identity changes are battery-covered + source-audited; the deep test's full container suite is the remaining verification.

---

## THE EVIDENCE ARTIFACTS (the paths)

- .trident/container-test-results.json (the current honest BLOCKED state — the deep test replaces it).
- .trident/test-plan.md (the deep test's 7-scenario plan — preflighted READY).
- .trident/wave-audit/wave-1786180415711.md (the th-ctx extraction audit).
- Checkpoints/ALL_TOOLS_WORKING_TRIDENT_WAVE_GENERATOR_7_4/ (the checkpoint — the sync pending).
- SHIP_PACKAGE/ (the ship package — the sync pending).
- THEATRICAL_FIREWALL_OVERHAUL_SPEC.md + THEATRICAL_FIREWALL_TUI_TEST_SPEC.md (the contracts).
- ~/.config/opencode/skills/warhead-writing/SKILL.md (the warhead-writing skill — the full workflow).

---

## THE CONTAINER DEEP TEST'S PREPARATION EVIDENCE (the ready state)

### The fixture creation (the CT exec — the exact commands' outputs)
- The wave-fx manifest: FIX_OK d793aea8bf31 eb27986f1677 — the 2-agent slop manifest (fx-a + fx-b, the lines 3, the sha256 d793aea8...) + the fx-a.md/fx-b.md prompt files (the short content).
- The wave-dpl manifest: the 130-line DPL1 fixture (dpl-a.md — the sha256 eb27986f...) + the manifest's lines 130.
- THE LINES-GATE's TEST DESIGN: the wave-fx's lines 3 → the exemption NOT applied → the [TASK FIREWALL] blocks the slop (S5); the wave-dpl's lines 130 >= 125 → the exemption APPLIED → the dispatch passes (S6) — the architecture's intactness proven both ways.

### The model resolution (the switch-model's evidence)
- The failed attempts: 'DeepSeek V4 Flash (new)' + 'OpenCode Go' → the status bar showed the Zen (the race) + the 20-attempt verified:false. THE WRONG DISPLAY NAME.
- The successful: 'DeepSeek V4 Flash' + 'OpenCode Go' → attempts 1, verified:true, the status bar 'DeepSeek V4 Flash (2x usage) / OpenCode Go' — THE CORRECT DISPLAY NAME (the WARHEAD 14's args-check).
- THE LESSON: the tool was never broken — the wrong input raced; the correct name landed cleanly (twice).

## THE PROVEN-VS-CLAIMED TABLE (the deep test's pending items)
| The claim | The status | The evidence needed |
|---|---|---|
| The [WAVE VERBATIM] blocks the condensed IN THE CONTAINER | CLAIMED (the host proved it; the container pending) | S3's verdict in the artifact |
| The [WAVE BATCH] blocks the single IN THE CONTAINER | CLAIMED (the host proved it; the container pending) | S4's verdict |
| The lines-gate blocks the slop | CLAIMED (the source + the battery; the runtime pending) | S5's verdict |
| The DPL1 exemption passes the 130-line verbatim | CLAIMED (the source; the runtime pending) | S6's verdict |
| The question-ban blocks the subagent's question | CLAIMED (the source; the runtime pending) | a subagent's question attempt in the deep suite |

---

## THE HONEST NOTES' DETAIL (the state's nuances)

1. The 2daaba66-era intermittency was the provider's flakiness — the code surfaced it loudly (the loud-fail law) — the stall detector + the telemetry made the failures fast + named.
2. The deep test's earlier blockage was the model providers' exhaustion + the config fumbling — RESOLVED: the correct display name landed the Go cleanly. The current state: the model LIVE + the fixtures ready.
3. The 341de6f1-era 6/6 was not re-run on the final dist — the deep test's S1 re-verifies.
4. The theatrical + the wave-verbatim + the identity changes are battery-covered + source-audited — the deep test's full suite is the remaining verification.
5. The endpoint probes' timings: the streaming first-byte 1.0s vs the non-streaming 48.9s — the transport's geometry proven — the 45s window's kills were the buffering's fault, not the provider's.

## THE CONTAINER DEEP TEST'S ARTIFACT TEMPLATE (the per-scenario record)
```json
{
  "buildSha": "11fb42e7...",
  "containerName": "theatrical-fw-ct",
  "timestamp": "<ISO>",
  "scenarios": [
    {
      "name": "<the scenario>",
      "passToken": "<the token>",
      "failToken": "<the token>",
      "passTokenMatch": true,
      "failTokenAbsent": true,
      "toolResultContext": true,
      "timedOut": false,
      "verdict": "PASS"
    }
  ],
  "overallVerdict": "PASS",
  "scenariosPassed": "7/7"
}
```

---

## THE EVIDENCE'S REPRODUCIBILITY (the operator's verification path)

- The dist: sha256sum dist/index.js == 11fb42e7.
- The battery: cd src/tests && bun test → 175 pass / 0 fail / 638 expect.
- The container: trident-container-test action=alive containerName=theatrical-fw-ct → alive; the screenshot → the status bar's Go model; action=exec ls /root/OPENCODE_WORKSPACE/trident-tmp/ → the fixtures.
- The warheads: the disk WARHEADS.md (the 14 warheads) + the inline (src/identity/index.ts) + the AGENTS.md.
- The skill: ~/.config/opencode/skills/warhead-writing/SKILL.md (the full workflow).
