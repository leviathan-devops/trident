# DECISION CHAIN — Trident v4.4

## Key Decisions

1. v4.3.3-LIGHTWEIGHT is the baseline — cloned, not referenced
2. Semantic activation (not regex-only) for Poseidon Mode on/off detection
3. Auto-shutdown after tool completion — agent cannot re-activate
4. Trident primary IS the God boss — audits directly, no container during loop
5. Container testing is LAST STEP only — after 96%+ runtime grade
6. Trident_Build based on v4.7-hotfix-v2-fixed — focused build engineer, not 15MB Trident duplicate
7. Semantic theatrical fix is a SEPARATE surgical fix already deployed to v4.3.3
8. **NEW — 2-Layer Semantic Firewall: Layer 1 (CycleTracker at Poseidon level) prevents trust-me-garbage in the God Loop by tracking finding lifecycle and detecting stagnation. Layer 2 (BuildFirewall at Trident_Build level) enforces plan scope compliance via before/after snapshot diffs, real AST analysis via ts.createProgram(), and SHA256 evidence enforcement. Based on the Five Orders of Program Analysis (Runtime Grade Bible): Order 2 (AST), Order 3 (TypeChecker), Order 5 (Behavioral/snapshot diff).**
