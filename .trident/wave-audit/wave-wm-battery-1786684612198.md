# WAVE AUDIT — wave-wm-battery-1786684612198 (the post-deploy wave-manager battery)

> AUDIT DATE: 2026-08-14. AUDITOR: the orchestrator (the post-deploy verification).
> THE WAVE: wave-wm-battery-1786684612198 (alias `wm-battery`, projectToken
> `wm-battery-verify`) — ONE E1 agent (wm-battery-a1) dispatched via the batch
> form (background:true + the promptFile channel) against the DEPLOYED dist
> 79100c42eb141caa (the 2026-08-13 battery fixes + the CT model-cache hotfix).
> THE AUDIT SCOPE: the post-deploy wave-manager battery T1-T9 — the compact
> check-in, the registry cycle, the compact status, the pause, the kill-wave +
> the archive, the old tools gone, the release-by-alias, the multi-process
> state, the self-heal no-phantom — PLUS the live kill-directive observation.

## 1. THE WAVE RECORD

| field | value |
| --- | --- |
| wave id | wave-wm-battery-1786684612198 (the alias rides in the id) |
| alias / projectToken | wm-battery / wm-battery-verify |
| agent | wm-battery-a1 (E1, trident_explore) |
| dispatch | background:true + promptFile → task_id ses_00146a09effeoUWA1PVVPEyk2a (spawned) |
| lifecycle | generate → dispatch → status running → pause paused → kill-wave aborted → row → wave_archive (aborted, archived_at 1786685014951) |
| deployed dist | 79100c42eb141caa437b920e8207caa50133af76288e5e0d6f610535dfdc365c |

## 2. THE PER-HUNK VERDICTS — THE BATTERY (the observable state, not the prose)

| test | the observation | verdict |
| --- | --- | --- |
| T1 THE COMPACT CHECK-IN | the generate's returned checkIn = 'WAVE wave-wm-battery-1786684612198 (1 agents) READY — DISPATCH the returned batch form as ONE message. Track it via trident-wave-manager action=status waveId=wave-wm-battery-1786684612198 — the full per-agent state is retrieved on demand (never stored in context).' — NO stale wall, NO trident-wave-status/steer, NO 'CHECK IN every' | PASS (the T1 fix is LIVE) |
| T2 THE REGISTRY CYCLE | the batch dispatch (promptFile + background:true) SANCTIONED → the subagent spawned (task_id returned, state running) — the re-fire path works | PASS |
| T3 THE COMPACT STATUS | the status = {wave, alias: wm-battery, projectToken: wm-battery-verify, status: running, etaMs, etaConfidence, elapsedMs, agents: [{name, state, sessionId, lastActivity}]} — the compact one-liners, NO tails/parts/error-codes | PASS |
| T4 THE PAUSE | the pause → status 'paused' + the note (the sessions stay alive — the wave NOT killed) | PASS |
| T5 THE KILL-WAVE + THE ARCHIVE | the kill-wave → status 'aborted' + agents[0].state 'killed'; the sqlite: the waves table EMPTY for wm-battery + the wave_archive row (status aborted, alias wm-battery, token wm-battery-verify, archived_at 1786685014951) | PASS |
| T6 THE OLD TOOLS GONE | trident-wave-status + trident-wave-steer NOT in the toolset (the tool-not-found by construction) | PASS |
| T7 THE RELEASE-BY-ALIAS | release waveId=wm-battery → '[RELEASE] no dispatch registry found for wave wm-battery — nothing to release' — the alias RESOLVED (no unknown-alias error); the registry already cleaned by the kill-wave/wipe | PARTIAL (the alias-resolution works; the live-registry reset needs a fresh wave) |
| T8 THE MULTI-PROCESS STATE | the python SELECT (a separate process) read the waves + the wave_archive tables — the shared sqlite rows visible | PASS |
| T9 THE SELF-HEAL | /tmp/trident-engine.log: the LAST main-session-heal KICK = 2026-08-13T18:11:20Z (the PRE-fix era); ZERO kicks AFTER the 21:45 fix deploy; the 66 total kicks all pre-fix + failed 'tui dead' (no text injected) | PASS (no phantom kicks post-fix) |
| THE KILL DIRECTIVE (live) | the STUCK_NO_ACTIVITY directive fired DURING the battery for a stale wave (wave-wave-w2-legion-scaffold-2026-0-...) with the REWORDED text: 'kill + respawn (trident-wave-manager action=kill / action=respawn), steer (trident-wave-manager action=steer sessionId=), or wait. The evidence: ... (trident-wave-manager action=status waveId=...)' — NO 'wave-status kill', NO 'trident-wave-steer' | PASS (the directive fix is LIVE) |

## 3. THE COVERAGE MAP

| the battery item | the evidence | covered |
| --- | --- | --- |
| the compact check-in | the generate's returned checkIn verbatim | ✓ |
| the registry cycle | the dispatch sanction + the spawn | ✓ |
| the compact status | the status output | ✓ |
| the pause | the pause output | ✓ |
| the kill-wave + the archive | the kill-wave output + the sqlite SELECT | ✓ |
| the old tools | the toolset absence | ✓ |
| the release-by-alias | the release output | ✓ (partial) |
| the multi-process state | the separate-process SELECT | ✓ |
| the self-heal | the engine-log kick timestamps | ✓ |
| the kill directive | the live STUCK directive text | ✓ |

## 4. THE FINDINGS + THE HONEST REMAINDER

- FINDING W-1 (observed — the tracker sessionId gap, BUG-11): the tracker rows' sessionIds stay '' — the fired STUCK directive showed 'sessions: ; taskIds: none' for the stale wave — the after-hook propagation is the flagged follow-up (non-blocking for this battery).
- FINDING W-2 (observed): the STUCK_NO_ACTIVITY directive fired for a STALE wave from ANOTHER session (wave-wave-w2-legion-scaffold-2026-0-1786648085682, 596m past its ETA) — the tracker row from a different session's wave — the cross-session rows are visible (the multi-process reality); the orchestrator owns that wave's decision.
- The T2 subagent (ses_00146a09) was ABORTED by the T5 kill-wave before its report returned — the battery's T2 pass criterion (the spawn + the sanction) was met at the dispatch; the probe readback was a bonus, not a gate.
- No git operations were performed during this audit.
