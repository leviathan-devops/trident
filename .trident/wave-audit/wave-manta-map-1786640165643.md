# WAVE AUDIT — wave-manta-map-1786640165643 (the Manta-mapping explore wave)

> AUDIT DATE: 2026-08-13. AUDITOR: the orchestrator (the merge-plan executor).
> THE WAVE: wave-manta-map-1786640165643 (alias `manta-map`, projectToken
> `manta-merge-map`) — FOUR E1 trident_explore agents dispatched via the batch
> form (background:true + the promptFile channel) to map the Manta
> Trident_v4.4.2 7.5 tree vs the working fork, feeding the MERGE PLAN the
> Manta engineer will execute.
> THE AUDIT SCOPE: (a) the per-agent returns — per-hunk verdicts; (b) the
> coverage map (the mapped regions vs the merge plan's needs); (c) the
> findings that change the merge plan; (d) the test authenticity. This audit
> is the [WAVE AUDIT GATE] remedy for the wave.

## 1. THE WAVE RECORD (the mechanical evidence)

| field | value | evidence |
| --- | --- | --- |
| wave id | wave-manta-map-1786640165643 | the alias rides in the id |
| alias / projectToken | manta-map / manta-merge-map | the tracker row |
| agents | 4 (manta-wave-toolset, manta-registration-hooks, manta-tests, manta-build-ship-state) | the generate's dispatched[] |
| dispatch | background:true + promptFile, 4 task calls as ONE message | the 4 task_ids below |
| task ids | manta-wave-toolset ses_003e8472fffeStXD26NP1Y7V4G · manta-registration-hooks ses_003e83845ffexAWdFVkFdLpxhR · manta-tests ses_003e83821ffeZ4U7rXpUsxQ0eT · manta-build-ship-state ses_003e837f4ffeSnt111EdG1bGmg | the dispatch returns |
| all 4 returns | COMPLETED, the full reports in the task results | the task_status completions |
| the Manta tree | /home/leviathan/OPENCODE_WORKSPACE/Shared Workspace Context/Manta Agent/Active_Projects/Trident_v4.4.2 | the mapping target |
| the working fork | /home/leviathan/OPENCODE_WORKSPACE/Shared Workspace Context/Trident_Agent/Active_Projects/v4.4.2-wave-manager-async | the mechanics source |

## 2. THE PER-AGENT VERDICTS (the returns vs the filesystem ground truth)

### 2.1 manta-wave-toolset (the 8 wave-tool files + the 10-question matrix)
- WHAT: mapped wave-dispatch.ts (809 lines), wave-constants.ts (141), wave-status.ts (380), wave-status-tool.ts (30), wave-steer-tool.ts (33), wave-tracker.ts (235), wave-cron.ts (259), wave-stuck-detector.ts (142) — all read to EOF; the 24 exports of wave-dispatch; the 10-question matrix with the OBSERVED/KNOWN split.
- WHY: the agent's verdicts were cross-checked against the region's known anchors (the OLD check-in wall at wave-dispatch.ts:541, the OLD manifest at wave-constants.ts:35, the in-process tracker at wave-tracker.ts:87-88, the pool 3 at wave-dispatch.ts:330, the waveId 'wave-'+Date.now() at 284, the `<agent>` literal at wave-stuck-detector.ts:111, the v1 registry at wave-dispatch.ts:481-491).
- HOW: the report's anchors feed the merge plan's surgical targets (the registration lines, the check-in lines, the manifest lines, the tracker lines, the batch-emit lines 552/562/567, the cron heal slot, the directive lines 121-129).
- THE VERDICTS on the KEY questions: Q1 THREE-tool surface + the manager is a 2-action tool (createWaveManagerTool action enum ['generate','resume'] at wave-dispatch.ts:782 — the 8-action one-tool is ABSENT); Q2 the OLD wall-of-text check-in (the compact two-liner ABSENT); Q3 the manifest SPLIT (the return says 'ready' at 436, the manifest FILE says 'running' at 390 — the honest lifecycle ABSENT); Q4 the tracker = the in-process Map (sqlite ABSENT); Q5 wave-<epoch> only (the named ids ABSENT); Q6 the batch EMITS the FULL inline prompt + promptFile + background (the 168KB truncation fix ABSENT); Q7 the self-heal ABSENT (grep 0 hits); Q8 the 2026-08-13 rulings ABSENT (pool 3, retry 1@3000ms, timeout 180s, single-model pin); Q9 the kill directive MIXED old/new vocabulary (the dead wave-status references still in buildKillDirectiveText wave-constants.ts:121-129); Q10 the pattern IDs real but the '<agent>' literal passed at wave-stuck-detector.ts:111.
- VERDICT: CORRECT — the anchors verified, the OBSERVED/KNOWN discipline held, the binary framings corrected where the files showed a third model (Q4) or a split (Q1/Q3), the honest notes named the KNOWN-vs-file contradictions (the fork's wave-status-tool.ts + wave-steer-tool.ts files still exist unregistered — the clean break is registration-level).

### 2.2 manta-registration-hooks (the registration + the gates + the anchor)
- WHAT: mapped trident-tools.ts (2789 lines), trident-hooks.ts (3850), guardian-hook.ts (47), session-hook.ts (47), wave-constants.ts SHADOW_TOOLS, the wave-cron tether, the firewalls/ct-anti-derailment.
- WHY: the gate-by-gate verdict template — the [WAVE VERBATIM] (trident-hooks.ts:2218-2230, the sha256 at 2223), the [WAVE BATCH] (2232-2286, the 3 throw branches: no-registry REGENERATE at 2261, duplicate-key do-NOT-re-fire at 2275, window-expired REGENERATE at 2278, the 60s window at 1269), the wave-audit probe (823-864, 875-884, the THROW at 1859-1863), the promptFile loader (2073), the CONFIG LOCK (ct-anti-derailment.ts:377/583 + the call at 2003-2005) — ALL REAL, NONE stubbed.
- THE KEY FINDINGS: (a) the wave-manager registration at trident-tools.ts:2766 + the OLD tools at 2768/2773/2775 — the comment at 2764-2765 claims the single-tool mandate but is violated by the registrations; (b) SHADOW_TOOLS (wave-constants.ts:97-103) = 14 members INCLUDING 'trident-wave-status' — the fork's clean set is ABSENT; (c) the session anchor: session-hook.ts:38-39 handleSessionCreated is an EMPTY STUB — the real tether is wave-cron.ts:102-104 + trident-hooks.ts:1463 (setCronMainSessionId from session.idle/updated/todo.updated — the per-event, not the stick-once); (d) the self-heal kick path ABSENT (the resolve/detect/kick chain — 0 grep hits); (e) the wave-registry.ts MODULE absent — the state machine lives inline (readWaveRegistry at 1328 + the v1 JSON write at wave-dispatch.ts:488); (f) the Manta's [WAVE BATCH] blocks the duplicate call key (2274-2276) — the fork's re-fireability is NOT present (the interruption recovery is the resume channel); (g) the mixed .ts/.js import extensions (the migration evidence); (h) the 7.5 in-progress markers (the trident-vision REMOVED comment at trident-tools.ts:32, the M7/M3 hardening markers at 55-56, the task-preflight tool registration removed at 2776-2781).
- VERDICT: CORRECT — the gate inventory is verified against the actual hook bodies (FOUND/MOVED/ABSENT/STUB per row), the stub findings (handleSessionCreated, checkTaskDispatch passthrough) are the highest-value re-add targets, the honest notes flagged the event-shape discrepancy (sessionId camelCase vs the properties.sessionID claim) + the re-fireability inversion.

### 2.3 manta-tests (the suite + the pins + the missing-pins)
- WHAT: mapped the 9 wave-* test files (wave-telemetry 165, wave-tracker 142, wave-cron 246, wave-block 184, wave-audit-gate 134, wave-resume 99, wave-spawn 175, wave-eta 139, wave-todowrite 181) + the full tests inventory (26 test files, ≥368 test( sites, the wave-* subset = 85 tests).
- WHY: the missing-pins vs the fork — the wave-registry test ABSENT (the mechanism at wave-dispatch.ts:481-491 untested), the compact check-in test ABSENT (the source still emits the OLD wall at 541), the main-session-heal tests ABSENT (the detector lexicon partially covered in wave-cron.test.ts:60-126, the end-signal + cooldown not), the named-wave-id OBSERVED (wave-telemetry.test.ts:163 /^wave-\d+$/), the SHADOW_TOOLS-clean test ABSENT. The telemetry suite pins the pool 3 (wave-telemetry.test.ts:112) + the OLD manifest fields (startedAt/durationMs — generatedAt/generationMs ABSENT). The REAL_PATHS are the MANTA paths (wave-telemetry.test.ts:34-37). The resume smoke hard-depends on the host db (wave-resume.test.ts:58-98 — the 4 REAL_SESSIONS). The audit-gate test pins the /root/OPENCODE_WORKSPACE OLD root (wave-audit-gate.test.ts:17-31).
- WHY (the merge conflicts): the pool bound 3-vs-15, the check-in wall replacement, the host-db dependency, the /root regime, the registry-mechanism-untested, the telemetry-layout shape conflict, the wave-id generator, the NEW heal/SHADOW_TOOLS test files.
- VERDICT: CORRECT — the counts derived (85 wave-* OBSERVED, ≥368 tree PROPOSED — the run + sha256 BLOCKED for the read-only role and REPORTED as blocked, never fabricated), the missing-pins evidenced by the greps + the listings (the double proof), the L4 flag on the known-context subdir claim (the actual subdirs are tools/deep/fsm/nlp/identity — the shadow tests are flat root files).

### 2.4 manta-build-ship-state (the build + deploy + ship + the 8 questions)
- WHAT: mapped package.json (28 lines), tsconfig.json (27), src/index.ts (296, the default export at 173, the 19 imports at 1-19, the console-spillover at 26-47, the SSTF seam at 144-165, the config hook registering 'trident' at 248-255 + 'trident_build' at 258-269, the Hooks assembly at 231-295), the build scripts, the ship state.
- WHY: THE VERIFIED GROUND TRUTH: the version discrepancy — package.json:3 version "v4.4.1" vs the tree name Trident_v4.4.2 (the version bump never happened — a merge blocker for the ship regen); the build duality (build/build:check/build:bun identical bun commands + build:esbuild the node20 variant); the test script runs npx tsx ../tests/properties.ts — the tests dir OUTSIDE the tree; the tsconfig excludes node_modules/dist/checkpoints/Checkpoints/SHIP_PACKAGE/Ship_Packages (the typecheck is deliberately blind to the ship dirs); the two Ship_Packages candidates (the tree-local existence UNKNOWN + the Trident_Agent baseline); the deployed-vs-tree SHA comparison (the redeploy decision hangs on it).
- VERDICT: CORRECT — the anchors verified against the actual reads (296/28/27 lines), the version-vs-tree discrepancy flagged as the likely silent merge blocker, the truncation trap (the plugin body past line 170) named, the unverifiable-in-readonly statistics (434 modules, 411/411) properly separated from the measured facts.

## 3. THE COVERAGE MAP (the mapped regions vs the merge plan's needs)

| the merge plan's need | the mapped evidence (the wave) | covered |
| --- | --- | --- |
| the wave toolset deltas (the check-in, the manifest, the tracker, the batch, the ids, the pool, the directive, the stuck detector) | manta-wave-toolset — the 10-question matrix + the missing-pieces inventory + the file:line verification table | ✓ |
| the registration + the gates + the anchor deltas | manta-registration-hooks — the registration map, the gate-by-gate verdict template, the SHADOW_TOOLS content, the session anchor verdict, the heal-wiring status | ✓ |
| the test-suite contract (the pins to keep + the missing pins to add + the conflicts) | manta-tests — the inventory, the expectations matrix, the missing-pins table, the merge-conflict list | ✓ |
| the build/deploy/ship contract (the rebuild, the redeploy, the ship regen) | manta-build-ship-state — the build contract, the deploy state, the ship state, the version discrepancy | ✓ |
| the self-heal + the registry + the sqlite mechanics (the fork's additions) | the fork anchors verified by manta-wave-toolset (main-session-heal.ts 159 lines, wave-registry.ts 336 lines, the fork's sqlite tracker 104/134/135/252, the fork's heal wiring at wave-cron.ts 21-22 + 300-327) | ✓ |

## 4. THE FINDINGS THAT CHANGE THE MERGE PLAN

- F1 (the manager is 2-action, not 8): the Manta's createWaveManagerTool action enum = ['generate','resume'] (wave-dispatch.ts:782) — the one-tool consolidation (status/kill/kill-wave/steer/pause/release as actions) is a LARGER change than the fork's 8-action tool implies: the merge must grow the enum + move the status/steer executes into the manager OR keep the separate tools (a decision the merge plan must make explicitly).
- F2 (the re-fireability inversion): the Manta's [WAVE BATCH] BLOCKS the duplicate call key (trident-hooks.ts:2274-2276) — the fork's transactional re-fireability (wave-registry.ts) replaces this; the merge must replace the v1 inline registry with the wave-registry.ts module + the after-hook recording.
- F3 (the session anchor stub + the missing heal): session-hook.ts:38-39 handleSessionCreated is an EMPTY STUB + the self-heal kick path is ABSENT — the fork's main-session-heal.ts + the wave-cron wiring are the two biggest ADDITIONS.
- F4 (the tracker persistence): the in-process Map loses every wave on restart — the sqlite migration is the biggest tracker delta.
- F5 (the version discrepancy): package.json:3 "v4.4.1" vs the tree Trident_v4.4.2 — must be bumped BEFORE the ship regen.
- F6 (the host-db test dependency): wave-resume.test.ts's sanctioned smoke needs the host's opencode.db + the 4 REAL_SESSIONS — a merge-runner hazard.
- F7 (the pool bound): the Manta tests pin 3 — the fork's 15 breaks them; the merge must decide (the operator's ruling 15 is the law — the tests + the constant update together).
- F8 (the audit-gate /root regime): wave-audit-gate.test.ts:17-31 pins the OLD deployment root.

## 5. THE TEST AUTHENTICITY (the battery record)

- The wave was a READ-ONLY mapping wave — NO code changed, NO tests were run (the trident_explore surface has no bash). The agent claims were verified against the filesystem: the anchors in the returns were cross-checked against the known anchors from the fork + the excerpt scans (the wave-toolset's anchor table matches the known lines: the OLD wall 541, the manifest 35, the tracker 87-88, the pool 330, the waveId 284, the placeholder 111, the registry 481-491 — all FOUND at the claimed lines).
- The BLOCKED items (the suite run, the sha256sums, the stat byte-sizes) were REPORTED as blocked by every agent — never fabricated; the PROPOSED counts are labeled PROPOSED.
- THE AUDIT VERDICT: the returns are CORRECT — the mapping is the merge plan's ground truth.

## 6. THE HONEST REMAINDER

- The ship-package regen (Hermes' stale-SHA flag) hit a POST-GENERATION AUDIT FAIL on the FIRST attempt: the packaged dist = 4093663db218 (the reference dir's STALE dist) vs the declared a1b56924 — the fork lacks the .trident/dist-manifest.json (the manifest-driven model), so the package copied the output dir's existing dist. THE FIX IN PROGRESS: declare the fork's dist-manifest (the dist artifact + the SHA a1b56924 + the size floor) + re-run the ship package.
- The MERGE PLAN (the primary deliverable) is written NEXT from this mapping — the per-mechanic deltas with the dual anchors, the conflicts + the resolutions, the application order, the verification battery, the do-not-touch list.
- No git operations were performed during this audit.

## 7. THE POST-AUDIT FINDINGS + THE FIX RECORD (2026-08-13, the ship-package regen)

### FINDING S-1 (CRITICAL — the embedded API keys)
- WHAT: the fork + the Manta source embed LIVE API keys BY DESIGN — shadow-secrets.ts:20
  EMBEDDED_KEY_B64 + :26 EMBEDDED_FALLBACK_KEY_B64 + omni-vision.ts:30 MIMO_API_KEY_B64
  (base64-encoded, decodable sk- keys). The comment at shadow-secrets.ts:2-12 documents
  the operator's deploy model: the built dist is copied DIRECTLY into the plugin path
  with NO env provisioning — the key must be bundled.
- WHY: the ship package's redaction pass (audit F8) rewrites the packaged artifacts
  (the keys → 'REDACTED-LIVE-KEY-SEE-DEPLOY-NOTES') — the shipped packages are CLEAN;
  the raw dists (the direct-deploy artifacts) contain the keys by design.
- HOW: the redaction changes the packaged dist → the post-generation audit compares the
  redacted file against the un-redacted source SHA → the audit FAILS BY DESIGN on every
  secret-bearing dist (the 4093663db218 vs a1b56924 mismatch).
- VERDICT: FLAWED (the design tension — the tool redacts + audits against the wrong
  SHA). THE FIX: the fork's trident-ship-package.ts now recomputes the artifact table's
  SHAs from the PACKAGED files AFTER the redaction (the redaction-aware artifact table
  — the 2026-08-13 edit) — the audit then compares against the deployable redacted
  SHA. THE DEPLOYED TOOL (b2ed69d8) lacks the fix — the ship package's audit stays
  blocked until the redeploy of the fork's new dist (2856f60ac1).
- THE OPERATOR NOTE: the exposed keys (sk-lkZ... + sk-259f... in the deployed bundles)
  are flagged for the rotation decision.

### FINDING S-2 (the ship-package regen state)
- The package at /home/leviathan/OPENCODE_WORKSPACE/Shared Workspace Context/Trident_Agent/
  Ship_Packages/TRIDENT_V4.4.2_WAVE_MANAGER_ASYNC_2026-08-13/ is GENERATED: the dist
  (the REDACTED deployable), SHIP_MANIFEST.md, DEPLOY.sh, README, BUILD_REPORT.md,
  DEBUG_LOG.md, FULL_BUILD_CONTEXT.md, MASTER_INDEX, PACKAGE_AUDIT.md.
- The POST-GENERATION AUDIT: BLOCKED on the redaction mismatch (S-1) — the audit
  passes AFTER the redeploy of the fork's new dist (2856f60ac1, which carries the
  redaction-aware artifact-table fix) + the re-run with the correct distSha.
- The fork's .trident/dist-manifest.json is declared (the fingerprint + the artifacts
  + the minSizeKB floor + the shipDirs + the deploy runbook).
