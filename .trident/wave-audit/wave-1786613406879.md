# WAVE AUDIT — wave-1786613406879 (host-direct-test)

**Wave:** wave-1786613406879 · agent-wave-1786613406879-1 (trident_explore, E1)
**Purpose:** the DIRECT HOST VERIFICATION of the dispatch-authorization transactional fix.
**Audited:** 2026-08-13, after the subagent's background dispatch + completion.

## THE CHANGED HUNKS / FINDINGS

### H1 — THE HOST DEPLOYED THE NEW BUILD (the precondition)
- **WHAT:** the host's plugin dist at /home/leviathan/.config/opencode/plugins/trident/dist/index.js = 7d5ff7352069826ed273ad1e3f703ee386c7ab1a36e699f961786bbb3b1b10b6 (16,156,318 bytes).
- **WHY:** the deploy was the operator's action; the SHA confirms the new build (the pre-deploy host was 8cd97a79).
- **HOW:** the sha256sum + stat verified against the artifact record.
- **VERDICT: CORRECT**

### H2 — THE GENERATION ON THE HOST (the real shadow pipeline)
- **WHAT:** trident-wave-manager generated wave-1786613406879 (1 agent) via the host's shadow-brain; the batch form returned with the placeholder prompt + the promptFile channel + background:true; the generation telemetry showed status 'ok' (durationMs 266212).
- **WHY:** the generation must exercise the real host pipeline (the pool/retry/timeout/model-pin rulings).
- **HOW:** the wave manager's returned output + the prompt file (190 lines) + the manifest.
- **VERDICT: CORRECT**

### H3 — THE HONEST MANIFEST (the fiction killed)
- **WHAT:** .wave-manifest-wave-1786613406879.json showed agent status 'ready' + generatedAt + generationMs + requestedWaveId 'host-direct-test' — NO 'running', NO startedAt/finishedAt/durationMs, NO dispatchedAt.
- **WHY:** the bug-report §3 demanded the honest lifecycle (never the shadow-generation timings dressed as agent runs).
- **HOW:** the manifest file read.
- **VERDICT: CORRECT**

### H4 — THE EXACT-BUG CYCLE ON THE HOST (the core fix)
- **WHAT:** (1) the malformed dispatch (background:"yes") was REJECTED by the host runtime: SchemaError(Expected boolean | undefined, got "yes" at ["background"]) — NOT a [WAVE BATCH] block; (2) the registry after the rejection showed the call 'recorded' + the wave 'dispatching' — the authorization NOT consumed; (3) after the 60s window expired, the re-fire (background:true) was SANCTIONED — the gate ALLOWED it (no [WAVE BATCH] block) — the runtime accepted + spawned the subagent (ses_00584bfcbffejPEuGKp1b32aLJ); (4) the registry transitioned to 'accepted' + 'dispatched' (the after-hook confirmation, engine-log verified: "→ accepted (wave wave-1786613406879)").
- **WHY:** the exact bug was "the authorization consumed by a failed dispatch — the wave permanently bricked"; the fix makes the failed attempt re-fireable.
- **HOW:** the runtime's SchemaError + the registry file + the task tool's task_id + the engine log's confirmation.
- **VERDICT: CORRECT**

### H5 — THE RE-FIRE PROTECTION (no double-dispatch)
- **WHAT:** exactly ONE agent-wave session in the db (count 1); a THIRD dispatch was BLOCKED by the loader ([TRIDENT PROMPT FILE] unreadable promptFile ENOENT — the T.E.A. wipe removed the prompt file after the accepted dispatch).
- **WHY:** the double-dispatch protection must hold.
- **HOW:** the db session count + the third dispatch's block message.
- **VERDICT: CORRECT**

### H6 — THE ANCHOR = THE PROCESS'S SESSION (the multi-session fix)
- **WHAT:** the engine log showed MAIN-SESSION ANCHOR: ses_00b413134ffe21xTrRGN9WzPTj (the session.created tether); the spawned subagent's parent = ses_00b413 — the SAME anchored session; the anchor appeared once (the stick-once held).
- **WHY:** each process must heal ITS OWN session (the 8-parallel-TUI question).
- **HOW:** the engine log's anchor line + the db's parent_id of the subagent.
- **VERDICT: CORRECT**

### H7 — THE RECORD-PRUNE BUG (found by the host test — NEW FINDING)
- **WHAT:** the wave's manifest + registry were REMOVED mid-test — the WAVE-RECORD PRUNE's sort was DESCENDING by age (`b.age - a.age`), so the over-cap check (`tq >= CAP=20`) removed the array END = the NEWEST records — the wave being tested was the over-cap victim when the shared host tmp (8 sessions' waves) crossed the 20-record cap. The container never hit it (few records).
- **WHY:** the intent (per the comment) was to keep the newest 20 + remove the oldest; the implementation removed the newest.
- **HOW:** the manifest + registry vanished + the engine log's prune entries + the release-by-alias failure ("no dispatch registry found").
- **VERDICT: FLAWED — THE FIX: the sort corrected to ASCENDING (`a.age - b.age` — the newest first) so the over-cap removes the OLDEST.**

## THE SPEC-COVERAGE MAP
- The wave manager generate (H2, H3) — covered.
- The [WAVE BATCH] gate + the registry state machine (H4) — covered (the exact-bug cycle).
- The after-hook confirmation (H4) — covered.
- The re-fire protection (H5) — covered.
- The main-session anchor (H6) — covered.
- The record-prune hygiene (H7) — covered + the fix pending.

## THE BATTERY (the verification runs)
- The host plugin dist SHA: 7d5ff735 ✓ (matches the artifact record).
- The runtime SchemaError on the malformed background ✓ (the exact-bug first half).
- The registry: ready → recorded (rejected) → accepted/dispatched (re-fired) ✓.
- The subagent count: exactly ONE ✓.
- The engine log: the after-hook confirmation + the anchor + the prune entries ✓.

**VERDICT: 6/7 CORRECT, 1 FLAWED (H7 — the prune sort, fixed in trident-hooks.ts).**
