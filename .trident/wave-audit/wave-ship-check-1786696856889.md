# WAVE AUDIT — wave-ship-check-1786696856889 (the ship verification probe)

> AUDIT DATE: 2026-08-14. THE WAVE: wave-ship-check-1786696856889 (alias ship-check,
> ONE E1 agent) dispatched via the promptFile channel + background:true against the
> DEPLOYED dist 89d7a4c2665d3481. Task id: ses_0008d0135ffeWllbbw61TDGDA2 — COMPLETED.
> THE AUDIT: the subagent's return (the v4 module surface report) — per-hunk verdicts.

## 1. THE WAVE RECORD + THE DISPATCH VERIFICATION
- The generate's checkIn carried the VERBATIM clause live: "...DISPATCH the returned batch form as ONE message, VERBATIM: every task-call parameter INCLUDING promptFile (the schema omits it — pass it anyway; omitting it = [WAVE VERBATIM] block)..." — the check-in's new text is IN the deployed dist.
- The dispatch (promptFile + background:true) PASSED the [WAVE VERBATIM] gate — the subagent spawned (no block) — the verbatim-dispatch protocol verified live.
- The density-law enforcement fired first (the thin context args REFUSED by the shadow-runner's CTX_FLOORS — the re-fire with the dense args succeeded).

## 2. THE PER-HUNK VERDICTS (the subagent's module report)
| hunk | WHAT | WHY | HOW | VERDICT |
| --- | --- | --- | --- | --- |
| the export census (20) | all 20 exports (9 fns/7 types/4 consts) listed with the verified anchors (33-368) | the ship gate's surface evidence | the module's API | CORRECT — every anchor matched the grep |
| the STATE_DB_PATH/STALE_PRUNE_MS contradiction | adjudicated as module-private (304/305, no export) | the context args listed them as exports | the store's internals | CORRECT — the contradiction flagged honestly |
| the GENERIC fallback | located at resolveDocFilter:154 (inline, no registry entry) | the 200-floor calibration | the fallback path | CORRECT |
| the data flows | the resolution/accumulation/completion/decision/store paths all traced with the contracts | the gate's acceptance | the module internals | CORRECT |
| the coupling graph | the hook import (trident-hooks.ts:14) + the call (1498) + the tests + the bundle anchors (dist:230653/230787/257597) | the blast radius | the production wiring | CORRECT — the v4 SHIPS in the deployed bundle |
| the failure modes | the INCONCLUSIVE warn-skip never throws (262-264) + the draft-min throw (270-277) + the silent store fallbacks (named) | the loud-fail law | the error paths | CORRECT |

## 3. THE COVERAGE MAP
- The v4 module surface (20 exports) ✓ · the hook wiring ✓ · the bundle presence ✓ · the check-in verbatim clause ✓ · the dispatch gate ✓ · the density-law enforcement ✓.
- THE LIVE HOST BEHAVIOR (the multi-angle battery): the thin write → the draft-min throw + the DRAFTING row (16); the SPEC-classified content → the COMPLETE + the 3000-floor throw (206); the plain 225-line doc → the VERIFIED allow (226) — the v4's three transitions all live on the host.

## 4. THE HONEST REMAINDER
- The deployed dist = 89d7a4c2665d3481 (the parenthetical version of the batch-form clause); the stripped dist 1b6e52b24ced0220 (the parenthetical removed) is built but NOT deployed — the redeploy (the user's terminal) closes the delta.
- The container (ct-v4-test) continues the live R3/R4 probes (the unit pins cover them).

## 5. THE AUDIT GATE REFRESH (2026-08-14 — the trident-task build)
- VERDICT: the ship-check probe's return is CORRECT (the v4 module's 20-export census all verified); the wave manager's generate → check-in verbatim clause → promptFile dispatch all verified live. The trident-task build proceeds on this audit.
- coverage: the module surface, the hook wiring, the bundle presence, the check-in, the dispatch gate, the density-law enforcement.
