# WAVE AUDIT — wave-post-comp-1786638623355 (the post-compaction host battery, T1-T9)

> AUDIT DATE: 2026-08-13. AUDITOR: the orchestrator (the host battery executor).
> THE WAVE: wave-post-comp-1786638623355 (alias `post-comp`, projectToken
> `post-comp-verify`) — ONE E1 agent (host-test-a1) dispatched via the batch
> form (background:true, the promptFile channel) on the host-deployed dist
> b2ed69d8d946929d4c2ce4e09b4d6be15c4342cc9a84a60c67e009112fc85cc5.
> THE AUDIT SCOPE: (a) the subagent's return (the forensic report) — per-hunk
> verdicts; (b) the host battery T1-T9 results — the test authenticity; (c) the
> coverage map; (d) the findings + the remedy. This audit is the [WAVE AUDIT
> GATE] remedy for the dispatch.

## 1. THE WAVE RECORD (the mechanical evidence)

| field | value | evidence |
| --- | --- | --- |
| wave id | wave-post-comp-1786638623355 | the alias `post-comp` rides in the id (the named-id design) |
| alias | post-comp | the tracker row data JSON |
| projectToken | post-comp-verify | the tracker row data JSON |
| agents | 1 (host-test-a1, E1, trident_explore) | the generate's dispatched[] |
| prompt file | /home/leviathan/OPENCODE_WORKSPACE/trident-tmp/host-test-a1.md | 177 lines, 23,290 bytes, sha 509afca8e4a7dc6f5280284bebd3e9162eab9bfd5e8712c469a68b190a058bff (≥ the 125-line DPL1 floor) |
| dispatch | background:true + promptFile, ONE task call (the 1-agent batch form) | the runtime returned task_id ses_0040270c7ffe213ykfoPR21jgu, state running → completed |
| tracker lifecycle | registerWave (status running) → pause (status paused) → kill-wave (row → wave_archive, status aborted) | the sqlite reads below |
| tracker db | ~/OPENCODE_WORKSPACE/trident-tmp/trident-waves.sqlite (WAL) | SELECT reads |

## 2. THE PER-HUNK VERDICTS — THE SUBAGENT'S RETURN (the forensic report)

The subagent was a READ-ONLY trident_explore (the extraction of the probe file
/tmp/opencode/host-test-a1.txt). IT MADE NO CODE CHANGES — its only artifact is
the report (its final message). The audit therefore verdicts the REPORT's claims
against the filesystem ground truth.

| hunk (the report section) | WHAT | WHY | HOW (blast radius) | VERDICT |
| --- | --- | --- | --- | --- |
| §1 THE REGION MAP | the probe file's role/exports/structure/first tokens, anchored :1-:3 | the file is the battery's T1 payload carrier | the file feeds ONLY the battery acceptance; no importers | CORRECT (verified: the file reads exactly 3 lines; the tokens verbatim; grep export = 0) |
| §2 THE FILE:LINE VERIFICATION | HOST-TEST-A1 :1 / purpose: :2 / content: :3, all FOUND, byteCount 164 decomposed (13+53+98) | the spec's KNOWN MEASUREMENTS TABLE is the acceptance gate | the acceptance compares the returned numbers | CORRECT (the byte arithmetic is exact; the tokens include the colons) |
| §3 THE DATA FLOWS | the single generate → read → return path with the boundary contract (the three verbatim first tokens) | the wave-manager pipeline's contract is the exact-token report | the dispatch pipeline consumes the report | CORRECT (no transforms exist in the flat file; the path is accurate) |
| §4 THE COUPLING GRAPH | zero importers (no exports), one consumer (the pipeline) | grep export = 0 is the evidence of the empty graph | the "empty-but-contractual" coupling is accurate | CORRECT |
| §5 THE FAILURE MODES | (a)-(e) EXCLUDED, (f) the stat-4-lines artifact OBSERVED+reconciled | the honesty contract: every failure path verdicts POSSIBLE/OBSERVED/EXCLUDED | the verdicts are accurate per the observed state | CORRECT |
| §6 THE ARCHITECTURE POSITION | the terminal-leaf fit + no violations | the battery T1 is the acceptance evidence | accurate | CORRECT |
| §7 THE VERIFICATION OUTPUTS | the grep proofs embedded; bun + sha256sum BLOCKED (the read-only surface) + loudly disclosed | the honesty law: a blocked command is reported blocked, never faked | the blocked-command disclosure is itself the correct finding | CORRECT (the disclosure is the honesty the audit demands; the machine-check gap is closed by the byte arithmetic + the grep 3-match proof) |
| §8 THE HONEST NOTES | the stat artifact reconciled; the blocked commands disclosed; no deviations; READ-ONLY honored | the grounding contract | accurate | CORRECT |

OVERALL SUBAGENT VERDICT: CORRECT — the return matches the ground truth (the
file I created: 3 lines, 164 bytes, HOST-TEST-A1 / purpose: / content:), every
claim anchored, the blocked commands honestly disclosed, no invented content.

## 3. THE TEST AUTHENTICITY — THE HOST BATTERY T1-T9 (the runtime record)

| test | the claim | the evidence (the observable state, NOT the prose) | verdict |
| --- | --- | --- | --- |
| T0 THE STATE | the deployed dist = b2ed69d8; the suite 410/410; tsc 0 | python sha256 of ~/.config/opencode/plugins/trident/dist/index.js = b2ed69d8d946929d4c2ce4e09b4d6be15c4342cc9a84a60c67e009112fc85cc5; `bun test src/tests/` = 410 pass / 0 fail (1339 expect() calls, 24 files); `bunx tsc --noEmit` = exit 0 | PASS |
| T1 THE ONE-TOOL GENERATE | wave-post-comp-<epoch> id; the tracker row with the alias+token; the manifest ready; THE COMPACT CHECK-IN | the wave id = wave-post-comp-1786638623355 ✓; the tracker row (waves table) carries alias post-comp + projectToken post-comp-verify + status running ✓; the manifest records the honest ready/generatedAt/generationMs ✓; **the check-in = the compact line + the STALE WALL (the append at wave-dispatch.ts:556 referencing the REMOVED trident-wave-status + trident-wave-steer + the CHECK IN/poll/steer directives) ✗** | **PARTIAL FAIL — the check-in regression** (the compact-context fix is INCOMPLETE in b2ed69d8: buildCheckInText is compact, but the finalCheckIn appends the old wall) |
| T2 THE DISPATCH + THE REGISTRY CYCLE | the malformed call rejected (auth not consumed) + the re-fire sanctioned + exactly ONE subagent | the malformed task call (no promptFile) → [WAVE VERBATIM] block (the runtime-side rejection, NOT a [WAVE BATCH] dispatch-auth block — the authorization stayed unconsumed); the re-fire with promptFile + background:true → the subagent spawned (task_id ses_0040270c7ffe213ykfoPR21jgu) → completed with the full report; EXACTLY ONE subagent session | PASS (the block class was [WAVE VERBATIM], not the SchemaError — the same rejection semantics: the runtime rejected the malformed call + the sanctioned re-fire succeeded) |
| T3 THE COMPACT STATUS | wave + alias + projectToken + per-agent ONE-liners, NO tails by default | the status returned {wave, alias: post-comp, projectToken: post-comp-verify, status: running, etaMs, etaConfidence, elapsedMs, agents: [{name, state, sessionId, lastActivity}]} — no tails/parts/error-codes | PASS (observation: sessionId "" — the tracker row was written at generate; the runtime dispatch id has not propagated into the row) |
| T4 THE PAUSE | status → paused; the wave NOT killed | the pause returned status 'paused' + the note (the sessions stay alive); the subagent remained running (completed later) | PASS (observation: pausedAgents [] — the composite's no-abort fallback for live background sessions) |
| T5 THE KILL-WAVE | the row leaves the waves table + lands in the archive with status aborted | the kill-wave returned status 'aborted' + agents[0].state 'killed'; the sqlite: waves = [] (EMPTY), wave_archive = [wave-post-comp-1786638623355, status aborted, alias post-comp, token post-comp-verify, archived_at 1786639168282] | PASS |
| T6 THE OLD TOOLS GONE | trident-wave-status + trident-wave-steer do NOT exist | the tools are NOT in the running toolset (the tool-not-found by construction — a call attempt is impossible; the source's tool list no longer registers them) | PASS |
| T7 THE RELEASE-BY-ALIAS | the alias resolves → the registry resets | the release with waveId=post-comp returned '[RELEASE] no dispatch registry found for wave post-comp — nothing to release' — the alias RESOLVED (no unknown-alias error) but the registry file was already cleaned by the kill-wave/wipe | PARTIAL (the alias-resolution mechanics worked; the live-registry reset needs a fresh wave — retest below) |
| T8 THE MULTI-PROCESS STATE | a second process sees the wave (the shared sqlite) | the python SELECT (a separate process) read the tracker row WHILE the wave was running (the T3-era read showed the full row: wave/data/status/updated_at); the status call (a second invocation) saw the wave | PASS (the multi-process read is the sqlite evidence) |
| T9 THE SELF-HEAL | NO phantom kicks on healthy sessions; a real drop → the kick | /tmp/trident-engine.log: 45 MAIN-SESSION ANCHOR lines (the per-process real-session anchors — the stick-once); ONE kick at 2026-08-13T16:30:00.107Z targeting 'main-sess' — the detector's classifier ONLY fires on a FINALIZED newest part (step-finish/completed) + a lexicon-incomplete tail (the drop signature — NOT a phantom); the kick FAILED 'tui dead' (no text injected); the 10m cooldown enforced (main-session-heal.ts:141-142); the misfire hardening (the end-signal check at :72-83 + the bare mid-sentence-cut removal at :98-102) is IN the deployed source | PASS (with notes: one REAL-drop kick fired + failed to land; the target name 'main-sess' vs the anchored 'ses_alpha' — the per-process anchor/cooldown maps are independent, the mismatch is the multi-process reality, not a cross-process cross-kick) |

## 4. THE FINDINGS + THE REMEDY

### FINDING 1 (CRITICAL — the T1 check-in regression)
- WHAT: the generate's returned check-in = the compact line (buildCheckInText —
  the 2026-08-13 anti-derailment two-liner) PLUS the stale 2026-08-12 wall
  appended at wave-dispatch.ts:556 — referencing the REMOVED tools
  (trident-wave-status + trident-wave-steer) + the CHECK IN every 5-10 minutes
  poll/steer directives + "Manage the waves like a senior engineer".
- WHY: the compact-context fix replaced buildCheckInText but never removed the
  finalCheckIn append; the check-in text is UNPINNED (no test covers it), so the
  stale wall survived the build + the deploy.
- HOW: the check-in flows into EVERY generate call's returned output (the
  orchestrator reads it verbatim) — the stale directive text actively re-introduces
  the derailment the compact design killed + names dead tools.
- VERDICT: ARCHITECTURE-VIOLATION (the compact-context design is violated in the
  deployed dist).
- THE REMEDY: (1) wave-dispatch.ts:556 — finalCheckIn = checkIn (drop the stale
  append; the background fact rides in the batch form's background:true + the
  status tool); (2) ADD the compact-check-in test pin (the wave-telemetry suite:
  r.checkIn contains the compact markers + NOT the dead tool names/directives);
  (3) re-run the suite + rebuild + redeploy + re-run T1.

### FINDING 2 (OBSERVATION — the tracker sessionId propagation)
- WHAT: the tracker row's sessionIds stay [''] — the runtime dispatch's real
  session id (ses_0040...) never propagates into the tracker row.
- WHY: the tracker registers at generate (sessionId ''); the after-dispatch hook
  does not update the row's sessionIds.
- HOW: the status tool's per-agent one-liners show sessionId '' for running
  agents — the steer/resume need the real session id.
- VERDICT: FLAWED (non-blocking for this battery — the status/steer actions
  resolve the session via the runtime when needed; flagged for the next wave).
- THE REMEDY (next wave): the after-hook (the task-accepted observation) updates
  the tracker row's sessionIds with the real task session id.

## 5. THE COVERAGE MAP

| the battery item | the coverage (the audited evidence) | covered |
| --- | --- | --- |
| the subagent's report | all 8 sections verdicts in §2 | ✓ |
| the generate output | the wave id, the check-in, the batch form, the telemetry (T1) | ✓ |
| the tracker row | the sqlite SELECTs at generate/pause/archive (T1/T3/T4/T5/T8) | ✓ |
| the dispatch gate cycle | the malformed → [WAVE VERBATIM] → re-fire → spawn (T2) | ✓ |
| the compact status | the T3 output (the one-liners, no tails) | ✓ |
| the pause + the kill-wave | the T4/T5 responses + the archive row | ✓ |
| the old tools | the toolset absence (T6) | ✓ |
| the release-by-alias | the T7 honest no-op + the alias resolution | ✓ (partial — live-registry retest pending) |
| the self-heal | the engine-log anchors + the single kick + the source hardening (T9) | ✓ |

## 6. THE HONEST REMAINDER
- T1 FAILS on the check-in (FINDING 1) — the battery's central purpose (catch the
  incomplete compact-context fix) SUCCEEDED: the host deployment does NOT fully
  carry the compact-context design. The fix (the 556 append removal + the pin) is
  the next action, followed by the rebuild + the redeploy + the T1 re-run.
- T7's live-registry reset + T8's cross-TUI status remain to be re-tested after
  the fix (a fresh wave).
- The dist under test b2ed69d8 is the fork's dist (SHA-identical) — the source at
  the fork is the canonical; the fix lands there + rebuilds to the next SHA.
- No git operations were performed during this audit (the fork disk is the
  canonical state per the operator).

## 7. THE FIX RECORD (2026-08-13 — the T1 remedy EXECUTED + VERIFIED)

### THE FIX 1 — the compact check-in (wave-dispatch.ts:556)
- WHAT: removed the stale 2026-08-12 background wall from finalCheckIn — the
  check-in is now EXACTLY the compact two-liner from buildCheckInText.
- WHY: the wall referenced the REMOVED tools (trident-wave-status +
  trident-wave-steer) + re-injected the poll/steer directives the compact
  context design killed (the T1 battery finding).
- HOW: finalCheckIn = checkIn — the background fact rides in the batch form
  (background:true) + the status tool.
- VERDICT: CORRECT (the fix + the pin + the suite + the build all green).

### THE FIX 2 — the compact check-in PIN (wave-telemetry.test.ts)
- WHAT: the new CHECK-IN test — r.checkIn contains the compact markers
  (WAVE <id> / READY — DISPATCH the returned batch form as ONE message /
  trident-wave-manager action=status waveId=<id>) + NOT the dead tool names
  (trident-wave-status / trident-wave-steer) + NOT the directive wall
  (CHECK IN every / Manage the waves like a senior engineer / POLL task_status /
  The wave runs in the BACKGROUND).
- WHY: the check-in text was UNPINNED — the stale wall survived the compact
  fix because no test covered it.
- HOW: the wave-telemetry suite, test 6.
- VERDICT: CORRECT (the suite is 411/411).

### THE FIX 3 — the SHADOW_TOOLS + the kill directive (wave-constants.ts)
- WHAT: removed 'trident-wave-status' from SHADOW_TOOLS (the dead tool's name
  must not linger in the registry — the clean break); reworded
  buildKillDirectiveText to the ONE-tool actions (action=kill / action=respawn
  / action=steer sessionId= / action=status waveId=) — the 'wave-status kill' +
  'trident-wave-steer' dead references are GONE.
- WHY: the clean-break law (no backward compatibility, no dead references).
- HOW: the SHADOW_TOOLS set + the directive builder.
- VERDICT: CORRECT.

### THE VERIFICATION (the re-run battery)
- bun test src/tests/ → 411 pass / 0 fail (1348 expect() calls, 24 files).
- bunx tsc --noEmit → 0 errors.
- The rebuilt dist → a1b56924fb90c0ab61e4cebb9147375ae8c86cc561330def037d114fabd6c56f:
  finalCheckIn = checkIn ✓ · the compact marker ✓ · 'CHECK IN every' ABSENT ✓ ·
  SHADOW_TOOLS without trident-wave-status ✓ · the ONE-tool kill directive ✓ ·
  'wave-status kill' ABSENT ✓.
- THE HOST STILL RUNS b2ed69d8 (the pre-fix dist) — the T1 LIVE RE-RUN requires
  the REDEPLOY of a1b56924 to ~/.config/opencode/plugins/trident/dist/index.js
  + the plugin restart, then the fresh generate re-run (T1).
