# WAVE AUDIT — wave-dd-audit-1786685184734 (the 7.4-vs-current regression audit)

> AUDIT DATE: 2026-08-14. AUDITOR: the orchestrator.
> THE WAVE: wave-dd-audit-1786685184734 (alias `dd-audit`, projectToken
> `dd-gate-audit`) — FOUR E1 trident_explore agents mapping the operator's
> symptom (the doc-density gate not firing) against the 7.4 checkpoint
> ALL_TOOLS_WORKING_TRIDENT_WAVE_GENERATOR_7_4. Task ids: dd-gate-wiring
> ses_0013e9c3bffeaniSdFB3RvN3OX, firewalls-inventory ses_0013e9945ffeW0c6Qvp7GqU4Eg,
> changed-set-diff ses_0013e9656ffe4mrxkS8ZVxf3SE, gate-test-coverage
> ses_0013e9320ffeY29p12vIGGNDd7 — all COMPLETED.

## 1. THE PER-AGENT VERDICTS

| agent | WHAT | WHY | HOW | VERDICT |
| --- | --- | --- | --- | --- |
| dd-gate-wiring | the gate's 7-element anatomy + the H1-H5 hypotheses + the root cause | the operator's symptom's root-cause evidence | the merge engineer's surgical targets | CORRECT — H4 confirmed (the docFinalize gate at trident-hooks.ts:1625-1626 makes the per-type floors unreachable for unmarked writes), H1/H3/H5 refuted, H2 partially supported (the output?.args vs input.args shape suspect), the trident_build bypass found (index.ts:105-108) |
| firewalls-inventory | the 9-gate inventory + the last-firing evidence | the symptom-delta (isolated vs systematic) | the blast radius | CORRECT — NOT systematic: Poseidon 3666+, SSTF Phase B active, the wave gates live-fired; the DOC DENSITY (0 log hits) + the WAVE AUDIT (0 — discipline-satisfied/model-surfaced) uniquely quiet; the doc gate's inertness = the exempt list (context_management/) + the draft semantics |
| changed-set-diff | the dual-tree inventory + the existence diff + the regression ranking | the blast-radius map | the fix plan's targets | CORRECT — the fork ≈ 222 .ts (the 201 claim corrected), the gate bodies byte-identical (+22 = a MOVE), the SSTF overhaul (the ship gate + the transform seam + the evidence ingestion) ABSENT in the fork, the ship-gate.test.ts DELETED, the CONFIG LOCK 14→8 families |
| gate-test-coverage | the dual test inventories + the per-gate pin matrix | the suite-green-while-gate-dead question | the coverage-gap verdict | CORRECT — the doc gate NEVER PINNED in EITHER tree (the suite cannot detect its death); the ship-gate pins LOST, the ct-lexicon WEAKENED (8 vs 14), the [WAVE BATCH] PINNED fork-only; the fork measured 315 test( lines / 1045 expects (the 411/1348 claim corrected) |

## 2. THE COVERAGE MAP

| the audit need | the mapped evidence | covered |
| --- | --- | --- |
| the doc-gate's wiring + the root cause | dd-gate-wiring — the 7 elements + the H1-H5 verdicts + the 3 decisive anchors (1625-1626, 1629-1631, index.ts:105-108) | ✓ |
| the firewalls' delta | firewalls-inventory — the 9-gate verdicts + the engine-log last-firing timestamps | ✓ |
| the changed-set | changed-set-diff — the inventories + the diff + the ranked regression candidates | ✓ |
| the test coverage | gate-test-coverage — the pin matrix + the doc-gate unpinned verdict | ✓ |

## 3. THE FINDINGS (the honest report's core)

- F1 THE OPERATOR'S SYMPTOM — CONFIRMED: the [DOC DENSITY GATE] throw has ZERO engine-log occurrences (509K lines); the edit-skip WARN fired twice (both 2026-08-12, pre-deploy). The gate is IN the deployed dist (dist/index.js:257463/257478) but never fires.
- F2 THE ROOT CAUSE — H4 (the unreachable throw), BY DESIGN: docFinalize (trident-hooks.ts:1625-1626) requires the DOC-COMPLETE marker or a floor-clearing re-save; unmarked drafts get only the 20-line floor (1631); the exempt paths (1468-1473) cover context_management/ + checkpoints/ + generated_artifacts/ + .trident/ + skills/ + node_modules/; the multi-occurrence edit-skip (1627-1628) never throws; the trident_build bypass (index.ts:105-108) skips toolBeforeHook entirely.
- F3 NOT A REGRESSION FROM THE FORK: the 7.4 checkpoint carries the BYTE-IDENTICAL gate bodies (+22 = a MOVE); the inertness is the SPEC'S OWN DESIGN (the spec's Part 11.1 confirms drafts pass); the doc gate was NEVER PINNED in either tree — its death is structurally undetectable.
- F4 THE REAL REGRESSIONS (the fork vs the 7.4 checkpoint): (a) the SSTF SHIP GATE + the TRANSFORM SEAM REMOVED (semantic-smoke-firewall.ts:41-43, 259-304 + hooks + index.ts:136-165) + ship-gate.test.ts (223 lines, 25 tests) DELETED; (b) the CONFIG LOCK 14→8 CTX families (CTX-09..14 — auth-exfil/git-config/symlink/docker-mount/host-fs-bind/immutable-lock LOST); (c) the fork's suite = larger surface (77 vs 76 describes) but smaller gate-pinning surface.
- F5 THE OTHER GATES FIRE (NOT systematic): Poseidon 3666+, SSTF Phase B dozens, WAVE PROMPT FILE active, WAVE VERBATIM/BATCH live-fired, TASK FIREWALL live-fired, CONFIG LOCK live-fired (Aug 11).
- F6 THE F6 SUSPECT (unresolved): the gate reads output?.args (1462) while the API doc + sibling code read input.args — if the runtime delivers args on the input, the gate is inert for EVERY write. The merge engineer's first RUNTIME probe.

## 4. THE HONEST REMAINDER

- The F6 args-shape question is the ONE hypothesis that cannot be closed from source alone — the runtime probe (a deliberate thin-.md write + observe) decides.
- The merged suite's gate-pin restoration + the CONFIG LOCK expansion belong in the MERGE_PLAN_MANTA_2026-08-13.md follow-up (the Manta 7.5 retains the SSTF overhaul — the merge must NOT re-drop it).
- No git operations were performed during this audit.

## 5. THE AUDIT GATE MARKERS (2026-08-14 — the [WAVE AUDIT GATE] remedy)
- VERDICT: the wave's four returns are CORRECT — the doc-density gate is LIVE + wired + in the deployed dist, its per-type floor structurally unreachable (the v2/v3 DRAFT/FINALIZE design, byte-identical to the 7.4 baseline), the SSTF-overhaul + the CONFIG-LOCK losses are the intentional removals (the other session's fix), the doc gate was NEVER pinned in either tree.
- coverage: the wave's 4 agents covered the gate wiring (7 elements + H1-H5), the 9-gate inventory + the last-firing log evidence, the dual-tree changed-set, and the dual test inventories.
